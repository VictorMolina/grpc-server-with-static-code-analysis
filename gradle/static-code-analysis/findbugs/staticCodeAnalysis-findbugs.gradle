findbugs {
    toolVersion = '3.0.1'
    ignoreFailures = false
    excludeFilter = file('gradle/static-code-analysis/findbugs/excludeFilter.xml')
}

tasks.withType(FindBugs) {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}
