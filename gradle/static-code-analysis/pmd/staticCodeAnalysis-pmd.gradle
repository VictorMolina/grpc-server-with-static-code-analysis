pmd {
    toolVersion = '6.15.0'
    ignoreFailures = false
    ruleSetFiles = files("gradle/static-code-analysis/pmd/ruleset.xml")
}

tasks.withType(Pmd) {
    reports {
        xml.enabled = false
        html.enabled = true
    }
}
